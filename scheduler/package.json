{
  "name": "scheduler",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Background job runner for Pulse (streaks, badge awards, plan refresh).",
  "engines": {
    "node": ">=18"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(add ESLint config or use root shared config)\"",
    "test": "echo \"(add tests)\""
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
