# Test Environment Variables for Playwright E2E Tests
# Update these with your actual test user credentials

# Test user credentials - create a test user in your Supabase instance
# These credentials are used for authenticated E2E tests
TEST_USER_EMAIL=testuser@test.com 
TEST_USER_PASSWORD=testpassword

# Note: Make sure to create this user in your Supabase instance before running authenticated tests
